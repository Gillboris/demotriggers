pipeline {
	agent any
	stages {
		stage('Build') {
			steps{
				sh 'echo "Build completed."'
			}
		}
       stage('test'){
             steps{
                sh 'echo "Mensah Welldone"'
			 }
	   }
	}
}
